## Installation

```sh
pip install HiveQDataDriver
# extras:
pip install 'HiveQDataDriver[hiveq]'    # HiveQ Data API SDK (transport=HiveQ)
pip install 'HiveQDataDriver[kdb]'      # legacy kdb+ transport (qpython3)
```

**Pre-requisite:** pandas >= 1.0.

